# Metrics export acceptance review

Current checkpoint: #5117 is closed. PR #5183 head4d9bcead8728a467c960910e38a7258357f7563b passed all applicable exact-head hosted jobs in run35464220426, including Linux and macOS suites. Technical, security and public reviews passed. It merged into dev as fec3add6ceff1dcc0ab31b232d65efc12db451db at2026-09-19T20:20:36Z; ancestry and issue closure at20:20:59Z were verified. Cumulative dev CI remains a separate campaign requirement. No local execution was performed.

The entries below are chronological evidence. Earlier failures and pending states do not override this checkpoint; scoped passes are not whole-feature completion.

Issue #5117 remains open. Prepared implementation `7578198be2289966fa637e36c996faf9368c1cec` adds an opt-in bounded exporter, configuration, management routing, documentation and focused tests. Runtime aggregation and configuration/privacy/documentation reviews were assigned independently. No local execution was performed.

Runtime review found two required corrections. Buffered HTTP 200 responses with a failed or incomplete terminal body lose terminal status at the non-stream finalizer and can be classified as completed; parsed terminal metadata must reach the metrics recorder without overriding cancellation or read errors. The new focused cases call recorder/logging helpers or inspect source strings, so they do not prove real HTTP or WebSocket finalization. Add transport-level flows and authenticated scrape assertions for exact logical/physical counts, terminal outcomes and missing timing/cancellation cases.

Static review accepted bounded labels, the WebSocket duplicate-finalization guard, attempt send-count inputs, terminal-stream EOF handling, disabled-mode inactivity and fresh-owner reset semantics. These accepted parts do not offset the missing acceptance evidence. The implementation owner received the source anchors and must return a corrected immutable head for review and hosted CI.

The separate config/auth/privacy/docs review passed at the prepared head. It covered default-off strict validation and malformed-load behavior, restart activation, shared transport injection, management authentication before disabled404, fixed labels/finite values, English and seven translated documentation sets, and lifecycle/units documentation. Runtime aggregation and transport-level proof gaps above remain blocking; the scoped PASS is not full implementation acceptance.

Correction `fbbe437614` passed independent runtime re-review: bounded terminal metadata is propagated only at clean EOF; cancellation and read-error precedence remain. New real-server HTTP and WebSocket scrape regressions cover exact logical/send counts, failed/incomplete buffered responses, retry/recovery, EOF/cancellation, missing versus zero TTFT and disabled behavior. Combined with the earlier config/auth/privacy/docs review, no known static blocker remains. Hosted verification and integration with the current writer-ownership layer still need proof.

Published as [PR #5183](https://github.com/lidge-jun/opencodex/pull/5183), dev base, head `298ec5f1ce45fdd412eee3d14b6665a199e6eaeb`, after rebasing onto the landed ownership change. The terminal-metadata correction has an identical patch ID; only base-dependent union handling in the initial feature layer changed. Independent rebase/fixture compatibility review and new-head hosted CI are required. The PR is attached to the coordinator task and covers the complete #5117 endpoint scope.

Initial #5183 hosted run35450374594 failed four new real-flow cases. The first buffered-read-error fixture reports an uncaught injected error; following cases encounter a state-home ownership conflict. Owner received completed-job logs for diagnosis of the first failure and cleanup consequences, without weakening ownership or removing transport assertions. This observed execution failure takes precedence over the earlier source-only PASS.

A second completed hosted job (Linux shard3, job105916394852) identified one source-oracle mismatch: the WebSocket idle-timeout test expected the previous one-argument handler construction. Owner received the exact anchor to update alongside real-flow repairs while retaining explicit timeout and activation assertions. No test removal or policy relaxation was requested.

Independent rebase review found no additional integration loss: source/test blobs match the reviewed feature, overlap docs/inventories preserve both metrics and ownership, and production shutdown still releases ownership in final cleanup. The observed read-error fixture and subsequent home-conflict cascade remain owner-assigned repairs; their existence is not evidence to relax production ownership. The separate source-oracle mismatch also remains tracked until corrected.

Fixture/source-oracle corrections committed at `6158748a4cd2cf5ed70420e2aef56069994910e7`; independent review is scoped to error activation, real transport assertions, complete teardown and retained WebSocket idle-policy coverage. No passing execution claim until new-head hosted CI.

Independent review accepted `6158748a4cd2cf5ed70420e2aef56069994910e7`: real read-error activation occurs after the first delivered chunk, error precedence remains observable, server teardown is awaited with a failure-visible cleanup safety net, and WebSocket source assertions preserve idle policy and terminal/fallback checks. Current-head hosted jobs are running without observed failures at this checkpoint; no runtime success is claimed yet.

New-head hosted execution at6158748 removed the subsequent state-home conflicts but still failed two actual transport cases: the buffered error rejects the fetch before body consumption, and cancellation is scraped before the expected aborted count is visible. Owner and independent source reviewer are investigating deterministic transport/error handling and finalization observation, preserving exact counts and avoiding fixed sleeps or skipped assertions. Static fixture approval did not establish execution success.

Further source tracing corrected the fixture-only assumption: a pre-response handler rejection bypasses final request logging, so the metric is absent. Owner was asked to use the existing once-only finalization guard before rethrowing, preserving client-visible transport behavior and missing timing semantics. The separate cancellation count failure is a propagation race; a bounded upstream-cancel signal can establish the actual completion boundary without sleeps or relaxed counts. Both require new source/test review and hosted proof.

Production/error-boundary correction `bf462d6070977746a2c5eb377220ca0d80494aaa` is published on #5183. Independent re-review is focused on once-only pre-response failure finalization, unchanged client transport errors, timing/send facts and deterministic cancellation observation. Fresh hosted verification is required.

Review of the new exception finalizer found a concrete pre-response cancellation case: a client abort during a buffered read reaches the same catch but must classify as aborted, not a non-abort read failure. Owner was asked to choose the existing cancellation status/reason from the actual request signal, preserve once-only finalization and rethrow, and add a live pending-body abort regression while retaining the non-abort failure test.

Hosted run35451702720 atbf462 again falsified assumptions in the error/cancellation fixtures: the client fetch resolved rather than rejecting in the observed handler-error case, and the expected upstream cancel callback was not observed before the bounded deadline. Owner and reviewer were told to derive a deterministic real-server harness boundary from source and existing conventions, preserve the actual metrics assertions, and avoid changing assertions speculatively or raising timeouts.

Head `8b589b3114b82027e164f38e08dc580803c89c43` adds cancellation-aware status/reason to the pre-response finalizer and a real pending-body abort case. That correction is under narrow review. The earlier read-error and cancellation fixture execution failures are separate and remain unresolved until a deterministic harness and new hosted proof establish them.

The8b589 production cancellation branch passed static review. Its new synthetic upstream body, however, did not observe the outgoing request abort signal; a mock is not automatically coupled to real network cancellation. Reviewer supplied an existing repository signal-aware fixture precedent. Owner was asked to attach rejection handling immediately, explicitly model upstream abort propagation, synchronize actual server finalization and retain the complete metrics assertions instead of assuming one Bun transport error shape.

Fixture-harness redesign `052eedb5cd10f135f8760aed76a170168cffc624` is published. Independent review now checks that the expected error/cancellation boundaries are actually activated and synchronized rather than inferred from default client transport behavior. Exact metrics counts remain required, with fresh hosted CI.

Combined harness review confirmed final-log observers run after the corresponding metrics update. Two deterministic-test corrections remain: explicitly handle the expected test server error without suppressing unexpected errors, and start rejecting deadlines only at await sites so early subscriptions cannot produce unhandled timeout rejections. The signal-aware abort/cancellation activations were accepted.

Primary-source research (2026-09-20) opened the [Bun HTTP error-handling contract](https://bun.com/docs/runtime/http/error-handling) in the requested browser and read the [server reload reference](https://bun.com/reference/bun/Server/reload). A handler error without an explicit error response uses Bun's default response path; a dedicated error handler can supply a deterministic response. The reload reference alone does not prove an error-only option object is accepted by the project's pinned types. Owner was given these boundaries to implement a test harness without assuming a universal client rejection or altering production error semantics. No local server or test was run.

Hosted1c1c8229 confirms the pre-response client-abort regression now passes. The two already-tracked read-error/default-handler and streaming cancellation observation cases still fail; no new defect class is inferred. Owner has the completed-job evidence and the confirmed official error-handler contract.

Fixture correction d4ee3baf3b installs a narrowly scoped explicit test server error handler, asserts the exact expected error and absence of unexpected errors, and makes event subscriptions non-rejecting until bounded observation. Independent review is checking the full cancellation activation rather than assuming client abort automatically controls a synthetic upstream body. Production behavior is unchanged in this test delta.

The revised error-handler and observer harness passed review. The remaining SSE fixture still ignored the outgoing abort signal: production permits a bounded post-cancel drain, so an indefinitely open mock cannot establish prompt finalization. Reviewer corrected the earlier acceptance and traced the real signal path. Owner was asked to make the second SSE mock honor that signal, preserving both the production drain bound and the test deadline. No timeout increase was requested.

Final targeted SSE fixture correction `794948670241886841b6ce6e89524bf2a63c5c1b` explicitly connects the second synthetic response to the outgoing request abort signal. No deadline or metrics assertion changed. Narrow independent review and fresh hosted checks are pending.

Independent final narrow review passed the SSE abort linkage at79494867: it uses the actual outgoing signal, handles pre-aborted input, errors the pending mock once, and preserves deadlines and all counts. Earlier error-handler and non-rejecting observer corrections remain accepted. Only actual current-head hosted execution can establish runtime success.

Head79494867 completed applicable hosted CI, but final public review exposed a remaining classification case: successful WebSocket upgrades finalize with101 and were counted failed by the fallback200–399 rule. Main verified source and requested explicit101 success while retaining cancellation and terminal-error precedence, with an upgrade regression. Green CI does not override this concrete gap.

Head `28ab81c3e44b8bac548766702d3385efd8dbde8d` adds101 success classification after existing terminal/cancellation precedence, a real live-sideband upgrade scrape regression and explicit failed/incomplete101 precedence cases. Narrow independent review and fresh hosted verification are pending.

Independent review passed the final101 classification correction at `28ab81c3e44b8bac548766702d3385efd8dbde8d`. The real upgrade path and metric are exercised; cancellation and failed/incomplete terminal precedence remain unchanged. No known static blocker remains at this head, but applicable hosted checks are still running.

The101 correction at28ab81c3 passed independent review with real upgrade activation and retained terminal/cancellation precedence. Current-head hosted verification remains pending; the earlier79494867 green run is not substituted for this head.

Hosted28ab81c3 cleared the earlier read-error and cancellation cases, but the new live101 fixture failed before the WebSocket opened. Owner and reviewer are checking the route's actual admission/configuration prerequisites against an existing successful upgrade harness. The metrics classifier is not proven through that fixture until the real upgrade is activated; helper-only substitution or relaxed admission was not requested.

The live101 fixture rejection was traced to an ineligible synthetic provider: route admission refused before the injected local WebSocket factory ran. Owner received an existing canonical-key fixture precedent and a test-only setup correction, while retaining real upgrade/count assertions and unchanged production eligibility. The earlier static activation claim was corrected.

Independent review passed the canonical101 fixture correction at `009317c24d46f3e03156bac3fc5b4b468ddaebec`. It meets the actual realtime selector prerequisites while redirecting only the injected upstream dial to a real local test server, asserts one factory invocation, and retains status/count/precedence/cleanup checks. Current-head hosted CI remains required.

Exact-head hosted run35456050014 passed at009317c24d46f3e03156bac3fc5b4b468ddaebec. Final public and independent review nevertheless confirmed two uncovered corrections: the live-upgrade fixture creates its upstream before the cleanup scope, and six post-context live-sideband terminal exits omit request finalization (admission refusal, resolver exception, acquisition cancellation/timeout, pre-upgrade cancellation, upgrade exception and rejected upgrade). The owner is correcting those paths with preserved transport statuses and real-route regressions. Merge remains held; the green run is not full acceptance.

Correction bf6a073cd23e15278d802b8f42f1eb5c54701ac2 is published with a request-local idempotent live-sideband finalizer, the six missing terminal exits and real-route cancellation/upgrade-failure regressions. The standalone upstream now has an outer cleanup scope. Independent re-review is in progress and exact-head hosted run35457502555 is active; no new passing execution claim yet.

Independent bf6a073cd2 review accepted production status/metadata and once-only finalization across all six exits, plus the repaired fixture cleanup. The acquisition-cancellation/timeout branch still lacks actual-route regression activation. The owner received that focused coverage requirement and an existing deterministic timer fixture precedent. The already-added pre-upgrade cancellation and upgrade throw/refusal cases remain retained.

Hosted bf6a073cd2 run35457502555 completed with a single macOS shard1 timing assertion failure in tests/providers/codebuddy-adapter.test.ts:643: expected elapsed time below250ms, observed312ms. Independent source diagnosis is comparing fixture setup and runtime cleanup boundaries; the threshold is unchanged and no environmental waiver is claimed. Acquisition-path coverage remains separately required.

The completed bf6a073cd2 macOS shard2 log(job105935319960) directly confirms all four live-sideband regressions passed: successful101, pre-upgrade499 cancellation, upgrade exception502 and refused upgrade426. This is scoped execution evidence; the separate shard1 timing failure and acquisition-coverage requirement still prevent merge readiness.

Independent ffb7b16c06 review confirmed real activation of acquisition cancellation, captured production deadline, capacity refusal and resolver exception with once-only status/metrics assertions. One fixture isolation correction remains: resolver/timer spies must enter cleanup scopes before server setup can throw. Timing diagnostic5899641f94 preserves the raw250ms assertion and adds bounded failure-only phase evidence; it does not claim a root-cause fix.

Independent cleanup re-review passed a9f69ec0514cbdf0c4db97d64b0a05f363d7cfff: resolver and timer spies enter restoration scopes before server setup; nested cleanup covers early failures while preserving all accepted actual-route and once-only metric assertions. Production deadlines remain unchanged. Current-head hosted execution and integration remain required.

Current a9f69ec051 macOS shard2 job105943379816 executed all17 real HTTP/WebSocket metrics cases successfully, including the four newly required acquisition/capacity/exception cases. The job's only failure is the already-assigned current-dev file-size ratchet pair. This directly proves the scoped metrics flows at this head; it does not waive full applicable CI or the repaired-base integration requirement.

Repaired-base head4d9bcead8728a467c960910e38a7258357f7563b is published on c81d43053b. Range comparison preserves all reviewed implementation and fixture patches; the first feature commit differs only in the expected layout inventory context supplied by the integrated schema work. Its current hosted matrix is active.

Scoped semantic union review passed metrics4d9bcead against dev3fd293d841, candidate treefd3fc217ef8c491218251bd9dd90dcfe79b6da7b. The relay overlap preserves EOF-only buffered metadata, cancellation/read-error precedence, final send counts and once-only finalization, alongside the integrated refusal classification. It does not change the existing separation between synthesized wire refusal and request-log classification; combined-tree hosted verification remains distinct from this source verdict.

Incremental semantic review also passed against dev4853f90d9ac87f49d95a7370f9bdf7eca014af3f, candidate tree7f727ae7435b00d58738808e3b83455d46f9e92b. The new readiness and streaming changes preserve optional metrics initialization, first-output timing, terminal/cancellation finalization and send accounting; both layout inventories retain all registrations. Combined-tree hosted proof remains a separate integration obligation.

Publication correction: screenshot-only commitc99d32fa7e was briefly pushed to the metrics branch, then removed with an exact-lease correction back to reviewed4d9bcead87 and placed on the dashboard branch. Main verified the intermediate diff contained only eight PNG assets and no runtime/test changes. The owner acknowledged the mistake and froze the metrics head. Earlier run35463258955 and target control35464219463 were cancelled by subsequent publication events, not failed assertions. Current run35464220426 is the execution receipt to assess.

Final-wave source-union review passed metrics4d9bcead against dev98b9b344e155b527f5f9a2c12af5d4845329d3ef, tree053911ae0a17ca098dcc9ea39c4417844f0d13fa. Eight additive intersections preserve configuration and inventory fields. Compaction retains the parent logging context, signal, lease and send budget; outbound header defaults introduce no extra dispatch; terminal/finish handling remains intact. No concrete source blocker or mandatory rebase was identified. Full combined hosted verification remains required in the integration track.

Integration receipt: native stacks lookup returned an empty list; maintainer-integration preflight, current actor/dev base and immutable head were checked. The actual merged tree053911ae0a17ca098dcc9ea39c4417844f0d13fa equals the independently reviewed final-dev union. Target control35463257826 succeeded; later intermediate control35464219463 was cancelled. Full Windows shards and diagnostic control were event-inapplicable, not passing executions. Dev push run35467094926 tracks the integrated tree.

Cumulative integration receipt: devfec3add6ceff1dcc0ab31b232d65efc12db451db push run35467094926 attempt1 completed successfully with all applicable producers and aggregate. This verifies the metrics plus prior integration union at that dev tip; later campaign changes still need their own cumulative proof.
